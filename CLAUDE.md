# CLAUDE.md — Operating Instructions

> **READ THIS SECTION BEFORE ANYTHING ELSE.**
>
> This project's previous handoff document was generated by an LLM and described a
> system that did not exist. Folder trees, failure histories, and completed milestones
> were fabricated in fluent, plausible prose. The human acted on it for weeks.
>
> Therefore this repository operates under an **evidence rule**:
>
> **Every factual claim you write into a document, commit message, comment, or docstring
> must be tagged `[VERIFIED]`, `[INFERRED]`, or `[UNKNOWN]`.**
>
> - `[VERIFIED]` — you ran a command in this session and saw the output. Include the command.
> - `[INFERRED]` — strong reasoning from evidence you can point to. State the evidence.
> - `[UNKNOWN]` — you do not know. **This is an acceptable and expected answer.**
>
> If asked to produce a "complete" document and you lack evidence for a section, write
> `[UNKNOWN] — resolve by running: <command>`. **Do not fill the gap with plausible prose.**
> Filling gaps with plausible prose is the single failure mode that cost this project weeks.

---

## 1. Project Objective

Build a local-first sports news aggregator that pulls NBA and NFL news and game data from
multiple sources, removes duplicate stories, summarizes them, and delivers a periodic brief
to the operator's phone.

**Two goals, ranked. When they conflict, goal 2 wins.**

1. **Ship a working system.** A brief that actually arrives on a phone.
2. **Teach the operator system design and end-to-end development.** The operator is a Data
   Science student with limited project experience. A finished system he cannot explain is
   a project failure, not a project success.

Goal 2 has direct consequences for how you work. See §6.

---

## 2. Repository Status — Read Before Writing Any Code

`[VERIFIED]` **One repository, two branches** — not two directories, as this section
originally described. `main` is the clean rebuild; `legacy` is the frozen snapshot.

### `legacy` branch — the abandoned prototype (reference only, DO NOT EXTEND)

`[VERIFIED via directory listing, 2026-08-03]` 136 files, 28 directories. Contains at
minimum **nine concerns implemented two or three times over in parallel**. It has never
completed a single end-to-end run.

**You may read it (`git show legacy:<path>`). You may copy individual files out of it, one
at a time, after the operator has read them. You may not build on it, run its test suite as evidence of health, or trust
`HANDOFF.md` inside it.**

### `main` branch — the clean rebuild (all new work happens here)

`[VERIFIED]` **No longer empty and no longer a separate directory** — the clean rebuild is
`main` at the repository root, and `legacy` is a branch holding the 97-file snapshot. It
runs unattended on cron and delivers to Telegram. See `SESSION.md` for real state, the target
shape and `TASKS.md` for the order.

---

## 3. Constraints (non-negotiable)

| # | Constraint | Consequence for you |
|---|---|---|
| C1 | **Runs on the operator's own Windows PC via WSL2 (Ubuntu, Python 3.10).** Not cloud. | `[VERIFIED]` Residential IP, so `stats.nba.com` responds. No Docker, no Kubernetes, no cloud DB. Do not add them. |
| C2 | **Free and open-source wherever possible.** | No paid API tiers without explicit approval. No Apify actor runs without a cost estimate presented first. |
| C3 | **Must be publishable as an open-source repo.** | No ToS-violating code in the tree. No scraper that requires bypassing bot protection. No unofficial WhatsApp bridges. |
| C4 | **Agentic approach preferred over hardcoded.** | Prefer configuration-driven, adapter-based, LLM-mediated designs over long `if/elif` chains — **but not at the cost of C5.** |
| C5 | **The operator must understand every merged line.** | See §6. This overrides C4 and overrides speed. |
| C6 | **Claude Pro tier.** Finite weekly usage and context. | Do not generate long explanatory essays on routine work. Budget context deliberately. |

---

## 4. Verified Facts About External Services

These are researched and confirmed. Do not re-litigate them without new evidence.

### NBA data

- `[VERIFIED]` `stats.nba.com` blocks datacenter IP ranges (AWS, GCP, Azure, Heroku).
  Requests hang rather than erroring cleanly. It also sits behind Akamai bot protection
  that rejects requests by TLS fingerprint.
- ~~`[VERIFIED]` `cdn.nba.com` live endpoints (scoreboard, boxscore, play-by-play, odds)
  have **no such protection** and work from anywhere.~~ **CONTRADICTED 2026-08-03.**
  `[VERIFIED]` `curl https://cdn.nba.com/static/json/liveData/scoreboard/todaysScoreboard_00.json`
  returns HTTP 403 with an Akamai `errors.edgesuite.net` block page, tested twice (with and
  without browser headers) from the agent's tool sandbox, **and independently from the
  operator's own residential machine outside any sandbox.** Same result both times. The
  original claim was never actually re-verified live in this session before being restated —
  it was carried forward from the prior session's research as fact. `ADR-003` is reopened;
  do not build on `cdn.nba.com` as the critical path until a working endpoint or alternative
  source is found. **Resolved:** balldontlie, ADR-003. `cdn.nba.com` is not used anywhere.
- `[INFERRED]` Because of C1 (residential IP), `stats.nba.com` will work for this operator
  today. **But building on it makes the project undeployable and unusable by anyone who
  clones the repo, which violates C3.**

**Current decision: `balldontlie.io` for NBA game data.** Free, documented, meant for
third-party use. Requires a free API key (operator signup; see `.env.example`). Full
rationale in `docs/decisions/ADR-003-nba-data-source.md`.

### WhatsApp

- `[VERIFIED]` WhatsApp Business API moved to per-message pricing on 2025-07-01. Marketing,
  utility and authentication templates are billed from the first message. There is no free
  tier for business-initiated messages. Only service messages inside a 24-hour
  *customer-initiated* window are free.
- `[VERIFIED]` The API cannot be accessed directly; a Business Solution Provider (BSP) is
  required, and BSPs add per-message markup and/or monthly fees.
- `[INFERRED]` A scheduled unsolicited brief is business-initiated by definition, so it is
  billable on every send, forever. This violates C2.
- `[INFERRED]` The unofficial alternatives (Evolution API, Baileys, `whatsapp-web.js`)
  impersonate WhatsApp Web using a real personal phone number. They violate WhatsApp's terms,
  risk a permanent ban of the operator's personal account, and cannot be published (C3).

**Decision: Telegram Bot API for v1.** Free, official, no BSP, no template approval, no
business verification, no ban risk, ~15 lines to send a message. WhatsApp remains a possible
*second* delivery adapter behind the same interface if the operator later chooses to pay.
See ADR-002.

### Scraping

- `[INFERRED]` ESPN and HoopsHype scraping in a public repo is a licensing and ToS exposure,
  not merely a technical one. Before any scraper is written, check for an official feed,
  RSS, or public API for that source first, and record the finding.

---

## 5. Architecture Rules

1. **One concern, one module. No exceptions.** Before creating any file, run
   `grep -rn "class <Name>\|def <name>" --include="*.py" .` and confirm it does not already
   exist. `[VERIFIED]` The legacy repo failed because this rule was absent: it contains
   four article-schema files, three ORM-model files, two orchestrators, two normalizers,
   two DB connection layers, two WhatsApp modules, two routers and two entrypoints.
2. **One canonical schema module: `models/schemas.py`.** Every other module imports from it.
   If you find yourself defining an article shape anywhere else, stop.
3. **One entrypoint: `main.py`.** No secondary runners.
4. **Adapters convert; the pipeline never learns source shapes.** Each source gets exactly
   one adapter class that returns `list[NewsArticle]`. The orchestrator must remain unchanged
   when a source is added.
5. **Build the simplest thing that satisfies the current slice.** SQLite until it demonstrably
   hurts. No embeddings until title-level dedup demonstrably fails on a real captured pair.
   No async until a synchronous run is measurably too slow.
6. **Every external call is wrapped in try/except and returns an empty list on failure.**
   A dead source degrades the brief; it must never crash the run.

---

## 6. Preferred Workflow — the learning contract

This is the part that makes this project different from ordinary agentic coding.
**Follow it literally.**

### Who writes what — REVERSED 2026-08-05

> **This section previously specified the inverse** — the human writing signatures, type
> hints, docstrings and failing tests, the agent writing only bodies, and any file the human
> could not explain being deleted and regenerated (ADR-006).
>
> `[VERIFIED]` **The operator reversed it explicitly:** *"You'll write the code not me."*
> He reaffirmed it two days later when the old contract reappeared as a remedy: *"I'm not
> writing anything just guide me through the code."* ADR-011 §7 records the reversal.

- **You write the code, the tests and the documentation. The operator does not.**
- **You never instruct the operator to write code.** Not as a task, not as a remedy, not as
  an exercise. You may say once that writing something would aid understanding, as an option,
  then drop it. `[VERIFIED]` This has already been violated once, three days after the
  reversal, by proposing he rewrite `processing/dedup.py` after a failed knowledge check.
- **You explain as you go**, at the point a concept first appears, grounded in this codebase.
- **The operator reviews, questions and decides** — especially anything user-facing.
- **Understanding is still required and still outranks shipping** (§1). It is demonstrated by
  *explaining*, not by authoring.
- **When an explanation does not land, the code is too clever.** Simplify it. Do not retest
  the operator, and never delete working software over a failed explanation.

`OPERATING_RULES.md` §0 and §10 hold the full version. This section and that one agree; if
they ever drift, that is a bug in whichever was edited last, not a precedence question.

### Turn discipline

- **Small diffs.** One function or one file per turn. Never generate multiple new modules
  in a single response.
- **Stop and ask** when a decision has more than one defensible answer. Do not pick silently.
- **Before writing, state the plan in 2–4 lines and wait for confirmation** on anything that
  creates a new file or adds a dependency.

### Explanation budget (C6)

The operator originally asked for a three-layer (deep / thorough / surface) explanation of
*every* coding decision. `[INFERRED]` That would exhaust Pro-tier usage and context far faster
than the coding itself, and would go unread within days.

**Revised protocol:**

- **Routine implementation** → one line: what it does and why here.
- **Architectural decision points** (~one per working session: choice of library, storage
  engine, pattern, boundary) → full three-layer explanation, written to
  `docs/decisions/ADR-NNN-<slug>.md` using the template in that folder.
  - *Surface*: what changed, in one sentence a non-programmer understands.
  - *Thorough*: the mechanism, the alternatives, the tradeoff.
  - *Deep*: the underlying CS/engineering concept, and where else it shows up.
- **Concept gaps**: the operator has stated he can explain async/await and embeddings, but
  **cannot yet explain the adapter pattern / dependency inversion, or database migrations.**
  When either appears, teach it inline before using it. Do not assume.

### Things to avoid

- Do not generate a whole subsystem "so it's ready."
- Do not add a dependency without stating what it does and what it replaces.
- Do not write documentation asserting a feature works before it has run.
- Do not mark a task complete in `TASKS.md` without pasting the command output that proves it.
- Do not silently create a second module for something that exists.
- Do not restore anything from `HANDOFF.md`.

---

## 7. Commands

`[VERIFIED]` Environment: WSL2 Ubuntu, Python 3.10, `.venv`, `pytest` 9.x installed in legacy repo.
Everything below marked `[UNKNOWN]` has not yet been run in the clean repo because the clean
repo did not yet exist. `[VERIFIED]` **It exists and most of these now run** — `make run`,
`make dry-run` and `make check` are the commands actually used; see `SESSION.md` §3.

### Environment
```bash
python3 -m venv .venv
source .venv/bin/activate
pip install -r requirements.txt
pip install -e ".[dev]"          # [UNKNOWN] only if pyproject defines extras
```

### Run
```bash
python main.py                    # [UNKNOWN] single pipeline run — not yet written
python main.py --dry-run          # [UNKNOWN] fetch + dedup, print, do not send
python main.py --source nba_live  # [UNKNOWN] single-source run
```

### Test
```bash
pytest -v                         # full suite, verbose
pytest -v --tb=short              # short tracebacks
pytest tests/test_dedup.py -v     # single file
pytest -k "dedup" -v              # by keyword
pytest --cov=. --cov-report=term-missing   # requires pytest-cov
pytest -m "not network"           # skip live-network tests
```

### Format / lint / typecheck
```bash
ruff format .                     # formatter (replaces black)
ruff check . --fix                # linter with autofix
mypy .                            # [UNKNOWN] not yet configured
```

### Debug
```bash
python -i main.py                 # drop into REPL after run
python -m pdb main.py             # step debugger
python -X importtime main.py      # what is slow to import
```

### Forensics — use these on the legacy repo, and on this one periodically
```bash
find . -path ./.venv -prune -o -name "*.py" -print | xargs wc -l | sort -n   # stubs float to top
grep -rn "NotImplementedError\|TODO\|FIXME\|^\s*pass\s*$" --include="*.py" . # unimplemented
grep -rn "class NewsArticle" --include="*.py" .                              # duplicate schemas
find . -name "__pycache__" -type d                                           # which packages ever ran
git log --oneline | head -30
```

> **Bytecode forensics — remember this technique.** A `.py` file inside a package that has
> ever been imported leaves a `.pyc` in `__pycache__`. A package directory with **no**
> `__pycache__` contains code that has **never executed**, regardless of what any document
> claims. Caveat: a file run directly as `python file.py` does not leave its own `.pyc`.
> This is how the legacy repo's true state was established.

### Smoke test for NBA connectivity
```python
from nba_api.live.nba.endpoints import scoreboard
print(scoreboard.ScoreBoard().get_dict())
```

---

## 8. Testing Philosophy

- `[INFERRED]` The legacy repo has 12 test files and zero working features. **Test count is
  not evidence of health.** A test that only asserts imports resolve proves nothing; that is
  exactly what `test_ingestion_setup.py` (passing in 3.32s) actually proved.
- Every test must assert on **behaviour with real-shaped data**, not on structure.
- Network tests are marked `@pytest.mark.network` and excluded from the default run.
- Fixtures live in `tests/fixtures/` as saved real JSON/HTML payloads, captured once from
  live sources. Adapters are tested against these, never against the live network.
- **You write the tests.** `[VERIFIED]` This reversed on 2026-08-05 with the rest of §6.
  The operator reviews what they assert and says whether it is the right behaviour.
- A feature is "done" only when a test asserting its behaviour passes **and** the human can
  explain the implementation.

### Write the test after the change is checked, then prove the test

Set by the operator on 2026-08-27, after four tests in one day passed for reasons other than
the change they named.

The order is: **make the change, check it does what you think by running or measuring it, and
only then write the test.** A test written beside a change tends to assert what you intended
rather than what happened, and both look green.

Then, before the test is trusted, **turn the change off and confirm the test fails.** This is
not the final audit it has been treated as, it is the moment the test earns its place. Two
rules make the difference:

- **The thing you switch off must be the exact mechanism the test names.** A mutation
  somewhere adjacent proves nothing about this test.
- **A test that survives is wrong and gets rewritten, not supplemented.** Adding a second test
  beside a broken one leaves the broken one asserting nothing.

`[VERIFIED]` The four from 2026-08-27, all found by mutation and none by review:

| Test | Passed because |
|---|---|
| a reader poll is rejected | the article factory defaults to the community feed, where a different rule already rejected it |
| a flagged sentence is shown | the summary printed above it contains the same sentence |
| a lower-case nickname is left alone | the word appeared in the sources anyway, so the guard never ran |
| the model is released after a run | it asserted `"0s"`, which was the wrong value to want |

`[INFERRED]` Three of those four were assertions that could pass through more than one
mechanism. That is the shape to watch for: if a second explanation for green exists, the test
is not measuring what its name says.

---

## 9. Conventions

- **Naming:** `snake_case` for modules, functions, variables. `PascalCase` for classes.
  Adapters end in `Adapter`. Delivery channels end in `Channel`. No abbreviations in public
  names (`article` not `art`).
- **Formatting:** `ruff format`, default settings, 88-column line length. Run before every commit.
- **Type hints required** on every public function signature. This is partly for tooling and
  partly because writing the signature first forces the design decision to happen before the
  code. `[VERIFIED]` The original wording said "the human writes the signatures"; that is the
  superseded §6 contract. The rule survives the reversal, the attribution does not.
- **Docstrings:** one line stating *intent* on every public function. Longer only where the
  *why* is non-obvious.
- **Commits:** `<type>: <imperative summary>`, one of `feat:`, `fix:`, `test:`, `docs:`,
  `refactor:`, `chore:`. Two rules the operator set on 2026-08-17, both hard:
  - **256 characters maximum for the whole message**, subject and body together. Count it
    before committing. The long explanations this repo was writing belong in `TASKS.md`, not
    in git history that nobody reads twice.
  - **One commit per file.** A change touching three files is three commits. Do not bundle a
    code change with its test, or either with its documentation. Where that forces an order,
    commit the code first so the tree stays runnable (§9 branches).
  - This does not weaken §0. Claims inside a commit still carry `[VERIFIED]`, `[INFERRED]` or
    `[UNKNOWN]`, but the measurement itself now lives in `TASKS.md` and the commit points at
    the item. `[INFERRED]` A tag with no room for its evidence is worse than a short message
    that says where the evidence is, so name the task id when there is one.
- **Tags:** `git tag -a vX.Y.Z -F <file>`, and the message becomes the release notes.
  **Do not start the notes with the version or a headline.** `[VERIFIED]` 2026-08-17:
  `release.yml` already passes `--title "$GITHUB_REF_NAME"`, so the GitHub title is correct on
  its own, and `gh release view v0.1.2` confirms it reads `v0.1.2`. What made the release page
  look wrong was the first line of the notes body, which repeated the version and a headline
  after a dash. Open with a plain sentence about what changed instead.
- **Branches:** `main` stays runnable at all times. Work on `slice/<name>`.
- **Documentation:** ADRs in `docs/decisions/`. `SESSION.md` updated at the end of every
  working session. `TASKS.md` updated as tasks complete, with proof.

### How to write, everywhere

Set by the operator on 2026-08-17. This covers commit messages, tag notes, documents, code
comments, docstrings and replies in conversation. All of it.

- **Plain words over jargon.** Say the thing, not the term for the thing. Where a term is
  genuinely the clearest option, say what it means the first time it appears, then use it.
- **No em dashes. None.** Use a comma, a full stop, brackets, or restructure the sentence.
  This applies even where one would be grammatically neat.
- **Bold almost nothing.** Reserve it for a line a reader must not miss, and expect that to be
  rare. `[INFERRED]` The existing documents in this repo bold so heavily that the emphasis
  carries no information, which is the problem this rule fixes.
- **Sound like a person.** Write to a colleague who knows the project, not to a specification.
  Short sentences are fine. Hedging every claim is not, that is what the §0 tags are for.

`[UNKNOWN]` Whether the older documents get rewritten to match. They were written under the
previous style and are heavy with dashes and bold. Nothing here requires a sweep, so treat the
rule as binding on new text and on any passage being edited anyway.

---

## 10. Configuration and Secrets

`[UNKNOWN]` No configuration exists in the clean repo yet. Target shape:

- `.env` — **never committed.** Must be listed in `.gitignore` before any secret is written.
- `.env.example` — committed, contains keys with empty or placeholder values.
- `config/settings.py` — single module that reads `.env` via `python-dotenv` and exposes a
  typed settings object. **Every other module imports settings from here and nowhere else.**

Placeholders anticipated:

```
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
DATABASE_URL=sqlite:///sportwire.db
LOG_LEVEL=INFO
POLL_INTERVAL_HOURS=8
DEDUP_WINDOW_HOURS=48
# --- deferred, do not populate yet ---
APIFY_TOKEN=
OPENAI_API_KEY=
ANTHROPIC_API_KEY=
```

> `[VERIFIED]` The legacy `config/settings.py` has **no `__pycache__` entry**, meaning
> nothing ever imported it. Its documented "app-wide settings and thresholds" were never
> read by any running code.

**Before the first commit of the clean repo, verify `.gitignore` covers:** `.venv/`,
`__pycache__/`, `*.pyc`, `.env`, `*.db`, `.pytest_cache/`, `.ruff_cache/`.
`[UNKNOWN]` Whether the legacy repo ever had a `.gitignore`, and whether `.env` or bytecode
entered its git history, is **unresolved and blocks open-sourcing that repo.** Resolve with
`git log --all --name-only | grep -i "\.env\|\.pyc"`.

---

## 11. Dependencies

Only what a slice needs, added when the slice needs it.

**v1 (required):**

| Package | Why |
|---|---|
| `requests` | HTTP for `cdn.nba.com` and Telegram. Synchronous, simplest thing that works. |
| `pydantic` | Runtime validation of the canonical `NewsArticle` / `GameData` schemas. Catches malformed source payloads at the boundary rather than three layers deep. |
| `python-dotenv` | Loads `.env` so secrets never enter source. |
| `pytest` | Test runner. |
| `ruff` | Formatter and linter in one tool; replaces black + flake8 + isort. |

**v1 (standard library, no install):** `sqlite3`, `hashlib`, `difflib` (`SequenceMatcher`
for lexical dedup), `logging`, `datetime`.

**Deferred — do not install until the triggering condition is met:**

| Package | Trigger condition |
|---|---|
| `nba_api` | When live `cdn.nba.com` data proves insufficient. Note the C3 portability warning in §4. |
| `nflreadpy` | When NFL is added, i.e. after the NBA slice works end to end. |
| `sentence-transformers` | Only after a real near-duplicate pair is captured that `SequenceMatcher` failed to catch. Pulls in PyTorch (~2GB). |
| `psycopg2` / `pgvector` / `SQLAlchemy` / `alembic` | Only when SQLite demonstrably fails. Probably never at this scale. |
| `apify-client` | Only after cost estimate approved (C2). |
| `httpx` / `asyncio` | Only when a synchronous run is measurably too slow. |
| An LLM SDK | Only at the summarization slice, and only after deciding local vs. hosted. |

`[INFERRED]` The legacy repo installed most of the deferred column before proving the first
column worked. Do not repeat this.

---

## 12. When You Are Handed This File

0. Read `OPERATING_RULES.md` (agent workflow discipline) and `SYSTEM_INSTRUCTIONS.md`
   (advisor stance). This file owns project facts; those two own how you work and how you
   speak. Where they conflict with this file, **this file wins** — and say so out loud.
1. Read `SESSION.md` for current state, then `TASKS.md` for the next action.
2. Read `ARCHITECTURE.md` only when you need the target shape.
3. **Do not read `legacy/HANDOFF.md` as fact.**
4. Before your first code change, run the forensic commands in §7 against the current repo
   and report what you actually find. State discrepancies against these documents out loud.
5. Ask the human before creating any file.
